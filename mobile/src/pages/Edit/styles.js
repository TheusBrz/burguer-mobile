import styled from 'styled-components/native';
import { colors } from '~/styles';

export const AddIng = styled.Text`
  color: ${colors.white};
  font-size: 16;
  text-align: center;
  margin: 0 0 0 5px;
  width: 75%;
`;
